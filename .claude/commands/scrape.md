---
allowed-tools: Bash(python -m web_scraper scrape:*)
description: Scrape a URL using this project's web scraper
---

Run the web scraper against the provided URL.

```bash
cd /Users/chris.rudnicky/Development/68thandMaine/scraper-main && .venv/bin/python -m web_scraper scrape $ARGUMENTS
```

Use the Bash tool to execute the command above exactly as written. Do not modify the URL or add any extra flags unless the user specified them. Report the output when done.
